{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm --dir artifacts/artz-interior run build",
    "dev:artz-interior": "pnpm --dir artifacts/artz-interior dev --host 0.0.0.0 --port 4173",
 "start": "pnpm run dev:artz-interior",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@types/react": "^19.2.17",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3",
    "vite": "^5.4.0"
  }
}
